# Function to track savings goal progress
def track_savings_goal(savings_goal, savings_balance):
    progress = (savings_balance / savings_goal) * 100
    if progress >= 100:
        goal_achieved = True
    else:
        goal_achieved = False
    return progress, goal_achieved
